comandos

criar pastas
cd documents
mkdir workspace
cd workspace
 
entra no git
clonar projeto
git clone projeto
git clone link do projeto

se conectar no git
cd ..
cd../.. voltar duas vezes
git config --global user.name "seu nome"
git confi --global user.email seu email do git não use aspas no email


criar chave
ssh-keygen
crie uma senha
repita a senha
cd .ssh pasta oculta
ls
cat id
copia o id
volte ao git
va ate as configurações
va para ssh para autentica a chave coloque um nome de sua preferencia
cole a chave
e crie a ssh key
salve a chave

volte ao terminal
navegue ate a pasta do projeto
de ls
cd .git
ls
cat config
cd .. saia da pasta
code . va ate o vscode

va ate as crendiciais clicando no botão do windons e delete as do GitHub

volte o vs code
abra um novo terminal
e intale o git hub compilot e o pull request
envie suas informações para git
e se cadastre na sua conta do github


aula 2

git branch lista de todas as branch.
git branch develop  cria uma nova branch.
git branch.

git checkout developtroca para a branch criada.
git branch.
git pull origin develop "escolha sua branch" -puxa todos os dados do GitHub direto para a branch escolhida.




html

head cabeça do site 
seo= adiministrador do site
meta charset="UTF-8"
<meta name="viewport" content="width=device-width, initial-scale=1.0"> deixar resposivo
meta name = "description" content= comportamento
meta name = "keyword "
meta name = "author" informação

meta gera informação/meta dados 

body corpo 
todo o conteudo sera codificado

header Cabeçalho
a = ancora
h = titulos
<a href= "pagina2.html">link para outra pagina</a> exemplo
img scr= cont/imagem/imagem.png width 400
p = paragrafo
d = span

navd 
main
footer
html



linguagem de marcação

endereços de web

link caminho esterno



navegar entre as branch e enviado as modificações feitas nas branch

git pull Paulo.vibes
git checkout develop
merge Paulo.vibes 
push -u oringin develop
git checkout master
git merge develop
git push -u oringin maste

{
    function soma (a, b)
    return a+b
    }


navegar ate a o workspace
criar uma pasta mkdir nome de sua escolha
navega ate essa pasta recém criada cd nomealeatorio
git init
ls
cole o link do repositório
digite code . para ir pro vscode